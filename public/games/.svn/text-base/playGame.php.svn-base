<?php

session_start(); 
header("Cache-control: private");

include_once("../includes/include.php");

printHeader( "Prima Lingua :: Games!", "students", "../" );

$game_id = $_REQUEST['game_id'];
session_register( game_id );

if( $game_id != "" ) {
  // Store ID # in session vars for use in gameVars.php
  $_SESSION[game_id] = $game_id;
  
  // Search MySQL database for information on game with requested ID#
  $query = "SELECT * FROM games, games_info WHERE games.game_id='$game_id' AND games.game_info_id=games_info.game_info_id";
  $result = $handle->query( $query );
  $assoc = $handle->fetchAssoc( $result );
  
  echo "<center><strong>Play $assoc[game]!</strong></center><br><br>";
  echo "
    <OBJECT classid='clsid:D27CDB6E-AE6D-11cf-96B8-444553540000'
 codebase='http://download.macromedia.com/pub/shockwave/cabs/flash/swflash.cab#version=6,0,0,0'WIDTH='600' HEIGHT='400' id='game' ALIGN=''>
    <PARAM NAME=movie VALUE='$assoc[game_swf]/$assoc[game_swf].swf'>
    <PARAM NAME=quality VALUE=best>
    <PARAM NAME=bgcolor VALUE=#FFFFFF>
    <EMBED src='$assoc[game_swf]/$assoc[game_swf].swf' quality=best bgcolor=#FFFFFF WIDTH='600' HEIGHT='400' NAME='game' ALIGN='' TYPE='application/x-shockwave-flash' PLUGINSPAGE='http://www.macromedia.com/go/getflashplayer'>
    </EMBED>
    </OBJECT>
  </center>
  <br><br>";
} else {
  header('Location: ../index.php');
}

printFooter( "../" );
?>
